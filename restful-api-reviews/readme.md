Prerequisites::

`pip install Flask`

`pip install -U flask-cors`

`pip install flask-mysql`
***
Note: `id.env` file is stored separately

```
MYSQL_USER =  
MYSQL_PASSWORD = 
MYSQL_DB = 
MYSQL_HOST = 
```
